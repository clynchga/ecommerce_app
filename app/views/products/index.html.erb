<h2><%= "Here are all the products for #{@store.location}" %></h2>

<ul>
<% @products.each do |product| %>
	<li><%= link_to "#{product.title}" , "/stores/#{product.store_id}/products/#{product.id}" %></li></br>
<% end %>
</ul>
<p><%= link_to "Create a new product" , new_store_product_path %></p>